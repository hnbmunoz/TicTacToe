import { CreateElement } from "../../utilities/utility.js";

const PlayerX = (panel) => {
  panel.innerHTML = "X";
};

const PlayerO = (panel) => {
  panel.innerHTML = "O";
};

export { PlayerX, PlayerO };
